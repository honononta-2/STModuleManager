{
  "name": "star-resonance-module-tool-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm:build": "wasm-pack build wasm --target web --out-dir ../pkg",
    "dev": "npm run wasm:build && vite",
    "build": "npm run wasm:build && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.4.1"
  },
  "dependencies": {
    "tesseract.js": "^7.0.0"
  }
}
