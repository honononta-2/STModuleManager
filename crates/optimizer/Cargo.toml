[package]
name = "star-optimizer"
version = "0.1.0"
edition = "2021"

[features]
default = []
parallel = ["rayon"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rayon = { version = "1.10", optional = true }
